# Лабораторная работа №31-32. Введение в SQLite и Entity Framework Core

ФИО: Федотова В.С.

Группа: ИСП-232

Дата: 24.04.26

---

## Описание

В ходе лабораторной работы разработано приложение для управления списком задач. Реализованы поиск, фильтрация, пагинация, статистика, а также массовое обновление и удаление.

## Полезные команды dotnet ef

| Команда                                     | Описание                                              |
| ------------------------------------------- | ----------------------------------------------------- |
| `dotnet ef migrations add <Name>`           | Создать новую миграцию                                |
| `dotnet ef database update`                 | Применить все неприменённые миграции                  |
| `dotnet ef database update <MigrationName>` | Откатить / применить до указанной миграции            |
| `dotnet ef migrations list`                 | Показать список миграций с их статусом                |
| `dotnet ef migrations remove`               | Удалить последнюю неприменённую миграцию              |
| `dotnet ef migrations script`               | Сгенерировать SQL-скрипт миграций без применения к БД |

## Структура проектов

```
Lab31-32_EFCore/
├── TaskDb/
│   ├── Controllers/
│   │   └── TasksController.cs
│   ├── Data/
│   │    └── AppDbContext.cs
│   ├── Models/
│   │   ├── TaskItem.cs
│   │   └── TaskDtos.cs
│   ├── Migrations/
│   ├── appsettings.json
│   ├── appsettings.Development.json
│   ├── Program.cs
│   └── TaskDb.csproj
├── img/
├── .editorconfig
└── README.md
```

## Маршруты

| Метод  | Маршрут                    | Описание                                   |
| ------ | -------------------------- | ------------------------------------------ |
| GET    | `/api/tasks`               | Получить все задачи                        |
| GET    | `/api/tasks/{id}`          | Получить задачу по ID                      |
| POST   | `/api/tasks`               | Создать новую задачу                       |
| PUT    | `/api/tasks/{id}`          | Полностью обновить задачу                  |
| PATCH  | `/api/tasks/{id}/complete` | Переключить статус выполнения              |
| DELETE | `/api/tasks/{id}`          | Удалить задачу                             |
| GET    | `/api/tasks/search`        | Поиск задач по тексту, приоритету, статусу |
| GET    | `/api/tasks/stats`         | Получить статистику по задачам             |
| GET    | `/api/tasks/paged`         | Получить страницу задач (пагинация)        |
| GET    | `/api/tasks/overdue`       | Получить просроченные задачи               |
| PATCH  | `/api/tasks/complete-all`  | Пометить все невыполненные как выполненные |
| DELETE | `/api/tasks/completed`     | Удалить все выполненные задачи             |

## Миграции

| Название миграции  | Добавленные изменения                                  |
| ------------------ | ------------------------------------------------------ |
| `InitialCreate`    | Создание таблицы `Tasks` + начальные данные (3 задачи) |
| `AddDueDateToTask` | Добавление поля `DueDate` и эндпоинта `/overdue`       |

## Сравнительная таблица

| Концепция          | Хранение в памяти        | EF Core + SQLite                           |
| ------------------ | ------------------------ | ------------------------------------------ |
| Хранение данных    | static List<T> в RAM     | Файл .db на диске                          |
| После перезапуска  | Данные пропадают         | Данные сохраняются                         |
| Поиск по условию   | LINQ to Objects          | LINQ to Entities → SQL                     |
| Создание структуры | Не нужно                 | Миграции (dotnet ef)                       |
| Начальные данные   | Хардкод в коде           | HasData() в миграции                       |
| Получение данных   | list.FirstOrDefault(...) | await db.Table.FindAsync(id)               |
| Добавление         | list.Add(item)           | db.Table.Add(item) + SaveChangesAsync()    |
| Удаление           | list.Remove(item)        | db.Table.Remove(item) + SaveChangesAsync() |
| Масштабируемость   | Ограничена RAM           | Гигабайты данных                           |
| Транзакции         | Нет                      | Встроены в EF Core                         |

## Главные выводы

1. **EF Core** — это переводчик между **C#** и **SQL**. Вы пишете **LINQ**, он пишет **SQL**.

2. **Миграции** — это система контроля версий для структуры БД. Так же, как Git для кода.

3. **Code First** удобнее, чем писать SQL вручную: изменил класс → создал миграцию → база обновлена.

4. **SaveChangesAsync()** — ключевой момент. До него все изменения живут только в памяти.

5. **async/await** при работе с **БД** — это не опционально, а стандарт. Блокировать поток на время ожидания БД — плохая практика.
